#ifndef PROTOBUF_FORWARD_H
#define PROTOBUF_FORWARD_H

// We use quotes because we've added ${GENERATED_DIR} to the include path
#include "broker.grpc.pb.h"
#include "broker.pb.h"

#endif  // PROTOBUF_FORWARD_H
